# from inventory_report.inventory.product import Product


def test_cria_produto():
    pass
    """ p1 = Product(
        1,
        "bol",
        "magic toys",
        "2022-02-22",
        "2023-07-30",
        "1223356648899",
        "armazene em lugar arejado com temperatura até 60Cº",
    )

    p2 = Product(
        "",
        "car",
        "magic toys",
        "2012-02-22",
        "2024-07-30",
        "",
        "armazene em lugar arejado com temperatura até 60Cº",
    )

    keys = [
        "nome_da_empresa",
        "nome_do_produto",
        "data_de_fabricacao",
        "data_de_validade",
        "numero_de_serie",
        "instrucoes_de_armazenamento",
    ]

    p1_assert = []
    p2_assert = []

    p1_assert.append(type(p1.id) is int)
    p1_assert.extend(type(p1[key]) == str for key in keys)

    p2_assert.append(type(p2.id) is int)
    p2_assert.extend(type(p2[key]) == str for key in keys)

    assert all(p1_assert) is True

    assert any(p2_assert) is False
 """
